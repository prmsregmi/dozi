{
  "name": "@dozi/frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @dozi/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "typescript-eslint": "^8.20.0",
    "eslint": "^10.0.0",
    "prettier": "^3.5.0",
    "typescript": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
